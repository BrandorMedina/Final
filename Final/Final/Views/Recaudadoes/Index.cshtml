@model IEnumerable<Final.Models.Recaudado>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.recaudado1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Doctor.cedula)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.recaudado1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Doctor.cedula)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_doctor }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_doctor }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_doctor })
        </td>
    </tr>
}

</table>
